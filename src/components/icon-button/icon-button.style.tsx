import styled from 'styled-components';

const IconButton = styled.button`

`;

const PrimaryIconButton = styled(IconButton)`

`;

const SecondaryIconButton = styled(IconButton)`

`;

const TertiaryIconButton = styled(IconButton)`

`;

export { IconButton, PrimaryIconButton, SecondaryIconButton, TertiaryIconButton };
